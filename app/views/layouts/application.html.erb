<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content = "width-device-width, initial-scale-1.0"/>
  <title>MyBooze</title>
  <%= stylesheet_link_tag    "application", media: "all",
    "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <script src="//maps.google.com/maps/api/js?key=AIzaSyD0ECYk2-k960qU6qN1pD29zSLALZTGJr0"></script>
  <script src="//cdn.rawgit.com/mahnunchik/markerclustererplus/master/dist/markerclusterer.min.js"></script>
  <script src='//cdn.rawgit.com/printercu/google-maps-utility-library-v3-read-only/master/infobox/src/infobox_packed.js' type='text/javascript'></script>
  <%= csrf_meta_tags %>
</head>
<body>
  <input type="checkbox" id="nav-toggle" hidden>
    <div class="nav">
      <label for="nav-toggle" class="nav-toggle" onclick></label>
      <% if session[:user_id] %>
      <h2 class="logo"> 
                <%=link_to (image_tag current_user.profile.avatar(:thumb), :class => "avatar"), profile_path %>
                <%= current_user.profile.name %>

      </h2>
      <% unless current_user.meetings.empty? %>
        <ul>
        Мои встречи:
          <% current_user.meetings.each do |meeting| %>
            <li><%= link_to meeting.title, meeting_path(meeting) %></li>
          <% end %>
        </ul>
      <% else %>
        Запланированных встреч нет!
      <% end %>
      <% else %>
      <h2 class="logo">
        Войдите или зарегистрируйтесь!
      </h2>
      <ul>
        <li><%= link_to "Войти", login_path %></li>
        <li><%= link_to "Зарегистрироваться", new_user_path %></li>
      </ul>
      <% end %>
    </div>
    <div class="mask-content"></div>
    <main role="main">
    <header>
      <%= link_to image_tag("logo.png"), root_path %>
      <div id="login">
       <% if session[:user_id] %>
          Добро пожаловать, <%= link_to current_user.name, profile_path %>
        <%=link_to  "Выйти", logout_path, method: :get %>
       <% else %>
        <%=link_to  "Зарегистрируйтесь", new_user_path %>
        , или
        <%=link_to  "Войдите", login_path %>
      <% end %>
    </div>
  </header>
  <div class="<%= controller.controller_name %>">
      <%= yield %>
  </div>
</main>
</body>
</html>
