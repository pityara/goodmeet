<!DOCTYPE html>
<html>
<head>
  <title>Goodmeet</title>
  <%= stylesheet_link_tag    "application", media: "all",
    "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <header>
    <%= link_to image_tag("logo.png"), root_path %>
    <%= @page_title || "GOODMEET" %>
    <div id="login">
    	<% if session[:user_id] %>
    		Добро пожаловать, <%= link_to (User.find_by(id: session[:user_id])).name, lk_show_path %>
    		<%=link_to  "Выйти", '/sessions/destroy' %>
    	<% else %>
    		<%=link_to  "Зарегистрируйтесь", new_user_path %>
    		, или
    		<%=link_to  "Войдите", login_path %>
    	<% end %>
    </div>
  </header>
  <div class="<%= controller.controller_name %>">
      <%= yield %>
  </div>
</body>
</html>
