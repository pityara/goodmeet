<%= form_for profile, url: profile_path, :html => { :multipart => true } do |f| %>
  <% if profile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(profile.errors.count, "error") %> prohibited this meeting from being saved:</h2>

      <ul>
      <% profile.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Ваше имя:" %>
    <%= f.text_field :name, size: 15 %>
  </div>

  <div class="field">
    <%= f.label "Ваш возраст:" %>
    <%= f.text_field :age, size: 2, maxlength: 2 %>
  </div>

  <div class="field">
    <%= f.label "Ваш город:" %>
    <%= f.text_field :city, size: 20 %>
  </div>

  <div>
    <%= f.label "Фото" %>
    <%= f.file_field :avatar %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
